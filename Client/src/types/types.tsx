export type SearchResultsType = {
  trackType: string;
  albumUrl: string;
  artistArr: string[];
  durationMs: number;
  title: string;
  previewUrl: string;
  uri: string;
  albumImg: string;
  trackUrl: string;
  albumName: string;
};
